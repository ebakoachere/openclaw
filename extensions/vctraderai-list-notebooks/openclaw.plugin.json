{
  "id": "vctraderai-list-notebooks",
  "name": "VC Trader AI List Notebooks",
  "description": "Read-only workspace-scoped tool: List the workspace's notebooks (title, purpose, author, archived state). Calls the propfirm_manager BFF as the workspace owner (PFM_AGENT_TOKEN). READ_ONLY per ADR 0078.",
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": [
      "list_notebooks"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
