{
  "id": "vctraderai-enqueue-backtest-worker-job",
  "name": "VC Trader AI Enqueue Backtest Worker Job (Propose)",
  "description": "RETIRED: enqueue_backtest_worker_job was removed from the ADR 0078 allowlist (W10 B2). POST /api/v1/openclaw/stage refuses it with 403 openclaw_tool_forbidden before anything is persisted, so it stages nothing and cannot be proposed or applied.",
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": [
      "enqueue_backtest_worker_job"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
