{
  "id": "vctraderai-get-fresh-candles",
  "name": "VC Trader AI Get Fresh Candles",
  "description": "Read-only workspace-scoped tool: Historical candles for a symbol via the live read endpoints. The payload states its own source (broker / cache / warm) and age_seconds. Calls the propfirm_manager BFF as the workspace owner (PFM_AGENT_TOKEN). READ_ONLY per ADR 0078.",
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": [
      "get_fresh_candles"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
