{
  "id": "vctraderai-prop-rule-headroom",
  "name": "VC Trader AI Prop Rule Headroom",
  "description": "Workspace-scoped tool: STATIC prop-firm rule thresholds (profit target / max-DD / daily loss) for a variant + account size; never returns live room. Calls the propfirm_manager BFF as the workspace owner (PFM_AGENT_TOKEN).",
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": [
      "prop_rule_headroom"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
