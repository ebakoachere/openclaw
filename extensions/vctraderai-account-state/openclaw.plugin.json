{
  "id": "vctraderai-account-state",
  "name": "VC Trader AI Account State",
  "description": "Read-only inspector for workspace account state. Its accounts read is browser-session-only, so it fails for agent callers.",
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": ["account_state"]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
