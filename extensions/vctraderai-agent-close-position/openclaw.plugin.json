{
  "id": "vctraderai-agent-close-position",
  "name": "VC Trader AI Agent Close Position",
  "description": "Autonomously close a live OPEN position while the account is set to act autonomously; otherwise the call does not go through and NOTHING is staged for approval (execution_status 'downgraded' + lock_reason). Calls the propfirm_manager BFF as the workspace owner (PFM_AGENT_TOKEN) at /api/v1/workspaces/{ws}/live/positions/{position_id}/agent-close.",
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": [
      "agent_close_position"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
