{
  "name": "@openclaw/vctraderai-heartbeat-now",
  "version": "2026.6.30",
  "private": true,
  "description": "Queue an Agent Alpha heartbeat policy to run on the next scan. It rewrites the policy's status to 'enabled', so it restarts a stopped or paused policy.",
  "type": "module",
  "dependencies": {
    "typebox": "1.1.38"
  },
  "devDependencies": {
    "@openclaw/plugin-sdk": "workspace:*",
    "openclaw": "workspace:*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
