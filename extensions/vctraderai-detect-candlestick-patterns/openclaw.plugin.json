{
  "id": "vctraderai-detect-candlestick-patterns",
  "name": "VC Trader AI Detect Candlestick Patterns",
  "description": "Read-only workspace-scoped tool: pure-compute candlestick patterns over the warm M1 feed rolled up to a timeframe. Calls the propfirm_manager BFF as the workspace owner (PFM_AGENT_TOKEN). READ_ONLY per ADR 0078.",
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": [
      "detect_candlestick_patterns"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
